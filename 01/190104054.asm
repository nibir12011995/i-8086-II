SSEG SEGMENT STACK 'STACK'
     DW 50 DUP(?)
SSEG ENDS

DSEG SEGMENT 'DATA'
CHARS DB 00000111b, 01111100b, 01001111b, 01110110b, 01011011b
DSEG ENDS

CSEG SEGMENT 'CODE'
MAIN PROC FAR

    PUSH DS
    MOV AX, 0
    PUSH AX

    MOV AX, DSEG
    MOV DS, AX
    MOV ES, AX

	MOV DX, 2030h	
	XOR SI, SI
	
	MOV BX, 5

NEXT:
 
	MOV AL,CHARS[SI]
	OUT DX,AL   
	 
    ;for delay 
	 
	MOV CX, 01FFH
    LO: 
    LOOP LO
 
	 
	INC SI
	INC DX 
    
    DEC BX
    CMP BX, 0
    JG NEXT 
    	

RET
MAIN ENDP
CSEG ENDS 
END MAIN  

